import styled from 'styled-components';

const LayoutContainer = styled.div`
  .page-content {
    margin: 0 auto;
    max-width: 1224px;
    margin-top: 100px;
  }
`;

export default LayoutContainer;
